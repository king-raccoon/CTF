>If a block cipher is secure, there should be no way for an attacker to distinguish the output of AES from a random permutation of bits.
>
>What is the name for the best single-key attack against AES?

`crypto{biclique}`